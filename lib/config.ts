// Shared configuration

// API routes are now proxied through Next.js API routes
// This keeps the backend URL and API key server-side only
// Frontend should use relative /api/* paths

// For client-side: use relative paths to Next.js API routes
export const API_URL = '/api';

