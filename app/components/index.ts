// Re-export the landing components from the new (landing) route group to keep imports working
export { Navbar } from '../(landing)/components/Navbar';
export { FeatureCard } from '../(landing)/components/FeatureCard';
export { Footer } from '../(landing)/components/Footer';
